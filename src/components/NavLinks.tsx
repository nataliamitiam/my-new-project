export function NavLinks( { children }: { children: React.ReactNode } ) {
    return (
        <a href="#" className="text-gray-300 hover:text-white transition-colors duration-200">{children}    </a>
    )
}